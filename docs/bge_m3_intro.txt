bge-m3 是智源研究院（BAAI）开发的多语言嵌入模型。

bge-m3 支持超过 100 种语言，能够生成 1024 维的稠密向量。它同时支持稠密检索、稀疏检索和多向量检索三种模式。

bge-m3 的最大输入长度为 8192 token，适合处理较长的文本段落。模型参数量约 566M，F16 量化后约 1.2GB。

在 RAG 系统中，bge-m3 负责将文本转换为向量，用于后续的相似度检索。它的多语言能力使其特别适合中文场景。
