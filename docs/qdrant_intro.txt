Qdrant 是一个开源的向量搜索引擎，专门用于存储和检索高维向量数据。

Qdrant 使用 Rust 编写，具有高性能、低延迟的特点。它支持余弦相似度、欧几里得距离、点积等多种距离度量方式。

Qdrant 的核心功能包括：向量索引（HNSW 算法）、负载过滤、批量写入、量化压缩。它提供 REST API 和 gRPC 接口，默认端口分别为 6333 和 6334。

在 RAG 架构中，Qdrant 充当向量数据库的角色，存储文档的嵌入向量并提供高效的相似度搜索。
