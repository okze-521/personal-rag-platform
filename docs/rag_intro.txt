RAG（检索增强生成）是一种结合检索和生成的人工智能技术架构。

RAG 的核心思想是：在生成回答之前，先从外部知识库中检索相关文档，然后把这些文档作为上下文提供给大语言模型，让模型基于真实资料生成更准确的回答。

RAG 系统由三个主要组件构成：
1. 文档处理：将原始文档分块、向量化并存入向量数据库。
2. 检索：用户提问后，将问题向量化，在向量库中检索最相似的文档块。
3. 生成：将检索到的文档作为上下文，交给大语言模型生成最终回答。

RAG 的优势包括：减少模型幻觉、知识可更新（只需更新文档库）、可追溯答案来源、无需重新训练模型。
